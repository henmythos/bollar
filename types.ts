export interface ProjectStats {
  value: string;
  label: string;
}

export interface NavItem {
  label: string;
  path: string;
}

export interface PriceRow {
  unitType: string;
  size: string;
  price: string;
}
